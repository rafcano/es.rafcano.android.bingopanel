<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <GridView
        android:id="@+id/gridViewNumbers"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/buttonNewPlay"
        android:layout_below="@+id/textViewTitle"
        android:layout_centerHorizontal="true"
        android:numColumns="10" >

    </GridView>

    <TextView
        android:id="@+id/textViewTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:text="@string/title"
        android:textSize="36sp"
        android:textAppearance="?android:attr/textAppearanceLarge" />

    <Button
        android:id="@+id/buttonNewPlay"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:onClick="onClickNewPlay"
        android:text="@string/new_play" />

    <TextView
        android:id="@+id/textViewLastNumbersLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/buttonNewPlay"
        android:layout_alignRight="@+id/gridViewNumbers"
        android:layout_alignEnd="@+id/gridViewNumbers"
        android:layout_toRightOf="@+id/buttonNewPlay"
        android:layout_toEndOf="@+id/buttonNewPlay"
        android:text="@string/last" />

    <TextView
        android:id="@+id/textViewLastNumbers"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/buttonNewPlay"
        android:layout_alignRight="@+id/gridViewNumbers"
        android:layout_alignEnd="@+id/gridViewNumbers"
        android:layout_below="@+id/textViewLastNumbersLabel"
        android:layout_toRightOf="@+id/buttonNewPlay"
        android:layout_toEndOf="@+id/buttonNewPlay" />

</RelativeLayout>
